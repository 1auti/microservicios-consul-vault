spring:
 application:
  name: config-service
 cloud:
  config:
   server:
    git:
     uri: https://github.com/1auti/microservices-config
     default-label: main
     force-pull: true

server:
  port: 8888


 


